var targetRow = $(".rather_<%= @rather.id %>");

targetRow.find(".choice_<%= @choice.id %>_votes").text("<%= @choice.votes.count %>");

targetRow.addClass("disable-clicks");

targetRow.find(".choice-wrapper.choice_<%= @choice.id %>").addClass("active");
