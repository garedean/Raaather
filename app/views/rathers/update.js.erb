var targetRow = $(".rather_<%= @rather.id %>");

targetRow.find(".choice_<%= @choice.id %>").text(<%= @choice.votes %>);

targetRow.find("input[type='submit']").attr('disabled','disabled');

targetRow.find(".choice_percentage").show();
